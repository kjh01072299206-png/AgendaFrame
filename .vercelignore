# Vercel 프로젝트의 Root Directory 는 site 다. 따라서 배포에 필요한 것은 site/ 뿐이다.
#
# CLI 배포는 저장소 루트에서 실행해야 한다(site/ 에서 실행하면 Vercel 이 site/site 를
# 찾는다). 그러면 기본적으로 저장소 전체가 업로드 대상이 되어 tmp/ 만으로도 파일
# 한도 15,000 개를 넘긴다. 여기서 site/ 밖을 전부 걸러 업로드를 수백 개로 줄인다.
/*
!/site

# 빌드가 직접 만드는 것들은 올리지 않는다
node_modules
.next
