# Test fixtures

Store small, deterministic, sanitized fixtures in domain-specific subfolders.
Crawler fixtures should contain the source URL, capture timestamp, and selector
version in adjacent metadata. Do not refresh fixtures during an ordinary test.
