schema_version: 1
project_id: project-40bc06fc-fb4b-46b6-a10
region: asia-northeast3
bigquery:
  dataset: agendaframe
  location: asia-northeast3
  maximum_bytes_billed: 1073741824
  daily_query_bytes_limit: 5368709120
storage:
  private_body_bucket: project-40bc06fc-fb4b-46b6-a10-agendaframe-private
  delete_all_bodies_on: "2026-10-31"
vertex:
  location: global
  model: gemini-2.5-flash-lite
  prompt_version: 2.4.1
  schema_version: 3
  max_articles_per_run: 50
  max_articles_per_day: 200
  max_input_characters_per_article: 20000
  # Six frame dimensions plus evidence locators need enough room to avoid
  # truncated JSON responses. The batch guard still bounds article count and
  # total spend before any model call.
  max_output_tokens: 6000
  max_attempts: 3
  thinking_budget: 0
  # The implementation uses synchronous generate_content, so use Standard
  # Vertex pricing here. Batch/Flex can be selected separately after a verified
  # batch adapter is added.
  estimated_input_usd_per_million_tokens: 0.10
  estimated_output_usd_per_million_tokens: 0.40
cost_controls:
  vertex_monthly_target_usd: 5
  cloud_run_monthly_target_usd: 2
  project_monthly_alert_usd: 10
  estimated_daily_vertex_limit_usd: 0.25
cloud_run:
  cpu: 1
  memory: 512Mi
  task_count: 1
  timeout_seconds: 900
  max_retries: 1
publication:
  endpoint_path: /api/import/analyzed
  include_article_body: false
  include_original_html: false
